# Exports (local only)

Place generated files here:

- `stadtwohnungen_export_*.csv` — from `scripts/export_listings.py`
- `website_debug.html` — from `scripts/debug_crawler.py`
- Optional zip bundles for sharing

These patterns are gitignored so listing data is not committed.
